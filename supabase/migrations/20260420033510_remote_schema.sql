-- Neutralized: this file was originally an accidentally-committed `supabase db pull`
-- schema dump whose statements all duplicate DDL from migrations 0017 and 0018
-- (playbook_role enum, playbook_members table, can_edit/can_view_playbook functions,
-- member RLS policies, storage bucket policies, etc.).
--
-- On prod it was marked as applied in schema_migrations without re-running (schema
-- already matched). On a fresh preview branch DB it failed with
-- `type "playbook_role" already exists`, blocking every preview build.
--
-- Replaced with this no-op so:
--   - the schema_migrations ledger still sees this timestamp and doesn't think
--     anything regressed,
--   - preview branches can apply the full migration chain cleanly.
select 1;
